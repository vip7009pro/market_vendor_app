# web-react (React frontend)

## Purpose

Future web app to manage the same data as mobile.

## Plan

- Start online-only (simpler).
- Later add offline support using IndexedDB + the same outbox/pull protocol.

## Tech

- React
- React Router
- API client to `backend-node`

See:

- `../docs/sync/SYNC_ARCHITECTURE.md`
- `../backend-node/README.md`
